[project]
name = "product-service"
version = "0.1.0"
description = ""
authors = [
    {name = "Andressa Ribeiro",email = "andressarfn@hotmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi (>=0.116.1,<0.117.0)",
    "uvicorn (>=0.35.0,<0.36.0)",
    "httpx (>=0.28.1,<0.29.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.group.dev.dependencies]
isort = "^6.0.1"
black = "^25.1.0"
flake8 = "^7.3.0"
pytest = "^8.4.1"
pre-commit = "^4.3.0"
pytest-cov = "^6.2.1"
ruff = "^0.12.11"

